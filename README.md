# Camera Improvements for Nuclear Option

Adds various improvements to the camera and camera tools for Nuclear Option \
All settings can have default configured via config manager, and are accessible to change in the camera tools menu (hold L or camera change bind). \
You will find the settings at the bottom, the menu is now scrollable.

## Current Features
- Toggle for free and control camera clipping through terrain/structures (not water)